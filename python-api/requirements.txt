# Install: pip install -r requirements.txt  (in your venv; file ships with the repo: python-api/requirements.txt)
# CORS is handled in app.py (no flask-cors package).
flask>=3.0.0
python-dotenv>=1.0.0
requests>=2.31.0
gunicorn>=22.0.0
